export type user = {
    first_name: String,
    last_name: String,
    email: String,
    password: String,
    phone: String,
    address: String,
    image: String,
    isAdmin: Boolean,
    isBanned: Boolean,
}